from typing import List

def plusOne(self, digits: List[int]) -> List[int]:

    lst = list(map(int, input("Enter digits separated by spaces: ").split()))

    #number = int("".join(map(str, digits)))

    return(lst)

""""""